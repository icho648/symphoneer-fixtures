# Symphoneer Fixtures

Deterministic, dependency-light repositories and applications used to verify Symphoneer's end-to-end issue execution loop.

The fixtures are intentionally separate from product repositories. A fixture should make failures easy to attribute to the orchestration, workspace, agent, verification, review, or pull-request pipeline rather than to databases, Redis, cloud credentials, or third-party services.
