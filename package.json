{
  "name": "symphoneer-fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node --test dist/*.test.js",
    "check": "pnpm test"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
